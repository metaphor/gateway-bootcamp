server:
  port: 8071
spring:
  application:
    name: 'sp-gateway'
  cloud:
    nacos:
      discovery:
        server-addr: '127.0.0.1:8848'

management:
  endpoints:
    web:
      exposure:
        include: '*'