server:
  port: 8091
spring:
  application:
    name: 'sp-auth'
  cloud:
    nacos:
      discovery:
        server-addr: '127.0.0.1:8848'

management:
  endpoints:
    web:
      exposure:
        include: '*'